# Secrets (never commit these)
.env*
secrets.*

# Private keys — managed by 1Password, never store in git
id_*
!id_*.pub
*.pem
*.key
*.gpg
*.asc
# Claude
.claude

# OS and editor junk
.DS_Store
*.swp
*.swo
*~
